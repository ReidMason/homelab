version: "3.8"

networks:
  monitoring:
    name: monitoring

services:
  uptime-kuma:
    image: louislam/uptime-kuma:1
    container_name: uptime-kuma
    volumes:
      - /mnt/user/appdata/uptime-kuma/:/app/data
    ports:
      - 3001:3001
    network:
      - monitoring
