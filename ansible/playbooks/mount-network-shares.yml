---
- name: Mount network shares
  hosts: testhost

  tasks:
    - name: Install nfs common
      become: true
      ansible.builtin.package:
        name: nfs-common

    - name: Mount network shares
      become: true
      ansible.posix.mount:
        src: "{{ item.src }}"
        path: "{{ item.path }}"
        opts: "ro"
        state: mounted
        fstype: nfs
        boot: true
      with_items: "{{ paths }}"
