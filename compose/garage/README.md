# Setup

1. Run the `create-garage-toml.sh` script to generate the configuration file and copy it to your host.
2. Adjust the `docker-compose` file as required with the correct path mappings and run it on your host.
3.
