{{- if .Values.persistence.enabled }}
{{- include "common.persistentStorage" (dict "pvcName" "nats-data" "size" .Values.persistence.size "storageClass" .Values.persistence.storageClassName) }}
{{- end }}
