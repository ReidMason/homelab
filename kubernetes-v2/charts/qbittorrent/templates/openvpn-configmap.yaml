{{- if .Values.vpn.customConfig.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: qbittorrent-openvpn-custom-config
data:
  custom.conf: |
{{ .Values.vpn.customConfig.content | indent 4 }}
{{- end }}
