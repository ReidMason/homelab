apiVersion: v1
kind: Service
metadata:
  name: habit-tracker
spec:
  selector:
    app: habit-tracker
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
