image:
  repository: example/app
  tag: "1.0.0"
containerPort: 8080
externalSecret:
  enabled: true
  name: app-secret
  remoteKey: secret/prod/app
secretEnv:
  - envName: TOKEN
    secretKey: token
    property: token
