{{- if .Values.persistence.enabled }}
{{- if .Values.persistence.data.storageClass }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: matter-server-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.persistence.data.storageClass | quote }}
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
{{- else if and .Values.persistence.data.nfs.server .Values.persistence.data.nfs.path }}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Release.Namespace }}-matter-server-data
  labels:
    app.kubernetes.io/instance: matter-server-data
spec:
  capacity:
    storage: {{ .Values.persistence.size }}
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: {{ .Values.persistence.data.nfs.server | quote }}
    path: {{ .Values.persistence.data.nfs.path | quote }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: matter-server-data
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
  volumeName: {{ .Release.Namespace }}-matter-server-data
{{- end }}
{{- end }}
