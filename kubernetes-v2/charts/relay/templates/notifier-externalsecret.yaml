{{- if and .Values.notifier.homelab.externalSecret.enabled .Values.notifier.homelab.externalSecret.remoteKey }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Values.notifier.homelab.externalSecret.name | quote }}
spec:
  refreshInterval: {{ .Values.notifier.homelab.externalSecret.refreshInterval | quote }}
  secretStoreRef:
    name: {{ .Values.notifier.homelab.externalSecret.clusterSecretStoreName | quote }}
    kind: ClusterSecretStore
  target:
    name: {{ .Values.notifier.homelab.externalSecret.name | quote }}
    creationPolicy: Owner
    deletionPolicy: Retain
  data:
    - secretKey: {{ .Values.notifier.homelab.externalSecret.secretKey | quote }}
      remoteRef:
        key: {{ .Values.notifier.homelab.externalSecret.remoteKey | quote }}
        property: {{ .Values.notifier.homelab.externalSecret.property | quote }}
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
        nullBytePolicy: Ignore
{{- end }}
