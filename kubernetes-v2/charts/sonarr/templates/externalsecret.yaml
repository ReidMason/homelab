{{- if and .Values.homelab.metrics.externalSecret.enabled .Values.homelab.metrics.externalSecret.remoteKey }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Values.homelab.metrics.externalSecret.name | quote }}
spec:
  refreshInterval: {{ .Values.homelab.metrics.externalSecret.refreshInterval | quote }}
  secretStoreRef:
    name: {{ .Values.homelab.metrics.externalSecret.clusterSecretStoreName | quote }}
    kind: ClusterSecretStore
  target:
    name: {{ .Values.homelab.metrics.externalSecret.name | quote }}
    creationPolicy: Owner
    deletionPolicy: Retain
  data:
    - secretKey: {{ .Values.homelab.metrics.externalSecret.secretKey | quote }}
      remoteRef:
        key: {{ .Values.homelab.metrics.externalSecret.remoteKey | quote }}
        property: {{ .Values.homelab.metrics.externalSecret.property | quote }}
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
        nullBytePolicy: Ignore
{{- end }}
