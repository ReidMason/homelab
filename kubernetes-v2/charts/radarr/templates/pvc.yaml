{{- if .Values.persistence.enabled }}
{{- include "common.persistentStorage" (dict "pvcName" "radarr-config" "pvName" (printf "%s-radarr-config" .Release.Namespace) "size" .Values.persistence.size "storageClass" .Values.persistence.config.storageClass "nfs" .Values.persistence.config.nfs) }}
{{- end }}
