{{- if .Values.homelab.clusterIssuers.staging.enabled }}
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: {{ .Values.homelab.clusterIssuers.staging.name | quote }}
spec:
  acme:
    server: {{ .Values.homelab.clusterIssuers.staging.acmeServer | quote }}
    email: {{ .Values.homelab.acme.email | quote }}
    privateKeySecretRef:
      name: {{ .Values.homelab.clusterIssuers.staging.privateKeySecretRef | quote }}
    solvers:
      - dns01:
          cloudflare:
            email: {{ .Values.homelab.acme.cloudflare.dnsChallengeEmail | quote }}
            apiTokenSecretRef:
              name: {{ .Values.homelab.cloudflare.externalSecret.name | quote }}
              key: {{ .Values.homelab.cloudflare.externalSecret.secretKey | quote }}
        selector:
          dnsZones:
{{- range .Values.homelab.acme.dnsZones }}
            - {{ . | quote }}
{{- end }}
{{- end }}
