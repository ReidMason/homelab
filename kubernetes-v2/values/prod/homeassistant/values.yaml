replicaCount: 1

hostNetwork: true

image:
  tag: "2026.8.1"

persistence:
  config:
    storageClass: longhorn

homelab:
  ingressRoute:
    enabled: true
    host: homeassistant.skippythesnake.com
    forwardAuth:
      enabled: false
