apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: seerr
  namespace: seerr
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`seerr.skippythesnake.com`)
      kind: Rule
      services:
        - name: seerr
          port: 80
